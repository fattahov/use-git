Глобальные настройки:
 Загрузить и установить Git
  git config --global user.name "Your Name"
  git config --global user.email psyhodreams@gmail.com
  Add your public key
        
Следующие шаги:
  mkdir use-git
  cd use-git
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:fattahov/use-git.git
  git push origin master
      
Уже есть Git репозиторий?
  cd existing_git_repo
  git remote add origin git@github.com:fattahov/use-git.git
  git push origin master
      
Импортировать репозиторий Subversion?
  Нажмите здесь
      
Когда все готово:
  Продолжить